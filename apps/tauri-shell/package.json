{
  "name": "@archon/tauri-shell",
  "private": true,
  "version": "0.1.0",
  "description": "HarnessOS desktop shell — Tauri 2 wrapper around packages/web with native filesystem access. Mac + Windows.",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:no-build": "tauri dev --no-watch",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "tauri": "tauri",
    "icon:setup": "node scripts/copy-icons.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
